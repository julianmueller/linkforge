[project]
name = "linkforge-core"
version = "1.2.1" # x-release-please-version
description = "Universal Robotics Interoperability Core"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pyyaml>=6.0.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
